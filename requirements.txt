# Pinned; binary wheels (manylinux, cp311) so the amd64 image needs no compiler.
#
# `formulas` is EUPL-1.1+ and is the reason this service exists as its own
# deployment surface — see README.md § Licensing.
#
# openpyxl is pulled in by `formulas` anyway; it is named explicitly because
# evaluate.py imports it directly for the pre-flight formula scan.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
formulas==1.3.4
openpyxl==3.1.5
