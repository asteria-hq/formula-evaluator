# This suite is standalone. Having this file makes this directory pytest's
# rootdir, so `pytest tests` from here just works and inherits no configuration
# from a parent directory.
[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
